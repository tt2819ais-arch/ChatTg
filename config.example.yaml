# ─────────────────────────────────────────────────────────────────────────────
#  Конфигурация мульти-агентного брейншторма в Telegram
#  Скопируй этот файл в config.yaml и впиши свои значения.
#    cp config.example.yaml config.yaml
#
#  OPENROUTER_API_KEY можно задать здесь ИЛИ в .env (рекомендуется в .env).
#  Значение из переменной окружения (.env) имеет приоритет.
# ─────────────────────────────────────────────────────────────────────────────

openrouter:
  # Один ключ на ВСЕ "мозги". Получить: https://openrouter.ai/keys
  # Лучше оставить пустым здесь и положить ключ в .env (OPENROUTER_API_KEY=...)
  api_key: ""            # напр. "sk-or-v1-xxxxxxxx"  (или возьмётся из .env)
  base_url: "https://openrouter.ai/api/v1"
  # Опционально (OpenRouter любит эти заголовки для бесплатных моделей):
  http_referer: "https://t.me/your_brainstorm_bot"
  app_title: "Telegram Brainstorm Bots"

# Босс — его указания выполняются в первую очередь, любой его ответ выше мнения ботов.
boss:
  username: "MaksimXyila"     # без @
  telegram_id: 5426581017     # числовой id (узнать у @userinfobot)

# ─────────────────────────────────────────────────────────────────────────────
#  Параметры обсуждения (контроль цикла — чтобы не заспамить чат и лимиты)
# ─────────────────────────────────────────────────────────────────────────────
discussion:
  max_rounds: 3              # сколько кругов высказываний на одну тему
  delay_seconds: 3           # пауза между сообщениями ботов (анти-спам / анти-rate-limit)
  max_message_chars: 900     # ботам велим писать кратко (1 абзац)
  language: "русский"        # язык обсуждения

# Модель-«модератор»: решает после каждого круга — продолжать / спросить босса / финал.
# Используй стабильную бесплатную модель. Может совпадать с моделью любого бота.
moderator:
  model: "openai/gpt-oss-120b:free"

# ─────────────────────────────────────────────────────────────────────────────
#  Боты. Нужно 4 разных Telegram-бота → 4 токена у @BotFather.
#  model — любой id с https://openrouter.ai/models?max_price=0  (бесплатные :free)
#  Список бесплатных моделей у OpenRouter МЕНЯЕТСЯ — если модель пропала,
#  просто впиши сюда другую :free модель.
# ─────────────────────────────────────────────────────────────────────────────
bots:
  - name: "Стратег"
    telegram_token: "PASTE_TELEGRAM_BOT_TOKEN_1"
    model: "openai/gpt-oss-120b:free"
    listener: true            # ровно ОДИН бот слушает сообщения группы (этого достаточно)
    persona: >
      Ты — Стратег. Мыслишь логично и структурно, видишь картину целиком,
      раскладываешь идею на шаги, цели и риски. Говоришь по делу, без воды.

  - name: "Креатор"
    telegram_token: "PASTE_TELEGRAM_BOT_TOKEN_2"
    model: "meta-llama/llama-3.3-70b-instruct:free"
    persona: >
      Ты — Креатор. Генерируешь смелые, нестандартные идеи и неожиданные ходы.
      Расширяешь возможности, предлагаешь «а что если». Энергичный, вдохновляющий.

  - name: "Критик"
    telegram_token: "PASTE_TELEGRAM_BOT_TOKEN_3"
    model: "z-ai/glm-4.5-air:free"
    persona: >
      Ты — Критик. Быстро находишь слабые места, риски и дыры в идеях.
      Придирчив, но конструктивен: за каждой критикой — как починить.

  - name: "Реалист"
    telegram_token: "PASTE_TELEGRAM_BOT_TOKEN_4"
    model: "qwen/qwen3-next-80b-a3b-instruct:free"
    persona: >
      Ты — Реалист. Сводишь всё к выполнимости: ресурсы, сроки, деньги, здравый смысл.
      Отсекаешь фантазии, оставляешь то, что реально сделать.
